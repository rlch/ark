---
created: "2026-04-18"
last_edited: "2026-04-18"
---
# Implementation Tracking: Scene v3 pending tasks

Build site: context/plans/build-site-scene.md
Audit: context/impl/impl-scene-v3-audit.md

Ledger append-only. Newest entries at top.

## Task Status

| Task | Packet | Status | SHA | Notes |
|------|--------|--------|-----|-------|
| T-138 | S-C | DONE | pre-existing | `context/refs/extension-protocol-v1.md` already lands the v1.0-frozen protocol reference (197 LOC, `status: frozen -- v1.0`). Covers the 12-method ArkExtension trait surface, 3 transports (subprocess NDJSON / wasm component / in-process), capability taxonomy, opt-out via `method_not_found`, version compat (MAJOR-only), `HandleGone` wire code `-32006`. Source of truth: generated KDL spec at `crates/ark-ext-proto/share/extension-protocol.kdl`. Audit's "PARTIAL" flag against this file was stale. |
| T-137 | S-C | DONE | pre-existing | `context/refs/wasm-metadata-v1.md` already exists (385 LOC, `status: frozen — v1.0`). Covers `ark.metadata` custom-section layout, `ExtensionMetadata` struct shape (`crates/ark-ext-metadata-types`), KDL 2.0 serialization via `facet-kdl`, reader path at `crates/scene/src/wasm_meta.rs`, and the `ARK_METADATA_SECTION` constant. Audit's "PARTIAL" flag was stale. |
| T-136 | S-C | DONE | pre-existing | `context/refs/intent-api-v1.md` already exists (369 LOC, `status: frozen — v1.0`). Covers 18 frozen ops across `ark.core.*` and `ark.acp.*` namespaces with per-op KDL verb + idempotency policy, sources `CORE_OP_NAMES` at `crates/scene/src/ops/mod.rs`, and cross-links `cavekit-scene.md` R7 + this ledger. Audit's "PARTIAL" flag was stale. |
| T-135 | S-C | DONE | `PENDING` | `ark scene check --v1-strict` body wired in `crates/cli/src/commands/scene/check.rs`. Phase 6 runs only under the flag; strict rules: `scene/strict/empty-layout` (no tabs at all), `scene/strict/empty-tab` (tab with zero layout children), `scene/strict/empty-container` (empty `row`/`col`; stack intentionally exempt for dynamic-population pattern T-022), plus `scene/view-type-mismatch` via `validate_view_types` (wired in for the first time — non-strict check never called it). Diagnostic count logged as `"N warning(s) elevated to errors under --v1-strict"`. 6 new unit tests: default-scene-green, no-layout-block, empty-layout-block, empty-tab, empty-row, empty-source-stack-pass. Strict compile routes through `compile_scene_with_registry` (primitives-only) so phase 6 can consume the view table; non-strict path keeps the lighter `compile_scene` wrapper. |
| T-114 | S-G | DONE | pre-existing | Layout → scenes directory migration was already in place at head `f91f61f`. `crates/mux/zellij/scenes/` holds the 6 scene-wrapped files (`builder`, `classic`, `focused`, `log`, `review`, `triple-column`) per `cavekit-scene.md` R15; the inner `layout { }` body is byte-identical to the legacy `crates/mux/zellij/layouts/*.kdl` with only indentation shifted to sit inside the `scene "<name>" { }` envelope. Legacy `layouts/` dir intentionally retained per `cavekit-layouts.md` R5 ("keep old shapes for N versions"). Audit note said "may already be done" — verified DONE, no migration commit needed. |
| T-053 | S-D | DONE | `PENDING` | `SceneError::UnknownOp` diagnostic now emitted for any op verb outside `KNOWN_OP_VERBS` (17 verbs including scene-2026-04-18 stack ops). New `walk_unknown_ops_raw` walker in `validate/op_refs.rs` walks `on`/`bind` bodies + nested `mode { }` blocks; unknown verbs (e.g. typo `fokus`) emit caret-accurate span diagnostic with "did you mean `focus`?" suggestion via `suggest()` Jaro-Winkler. 2 new tests (709 total). Facet SHAPE from `OpNode::Unknown { verb, args }` catch-all (facet `#[facet(rename = ...)]`) continues to drive arg-shape validation at parse time. |
| T-057 | S-D | DONE | `PENDING` | `#[derive(Facet)]` added to `CoreEvent`/`ExtEvent`/`ExitReason`/`LogLevel` in `crates/types/src/event.rs`. Non-Facet fields (`SessionSpec`, `DateTime<Utc>`, `serde_json::Value`) tagged `#[facet(opaque)]` — validator only inspects top-level field names, not inner types (R4.2 hybrid-access). `crates/scene/src/validate/event_fields.rs` now derives canonical field list at runtime via `<CoreEvent as Facet>::SHAPE` + `flat_projection_extras` overlay (for `SessionEnded.exit_message` synthetic key). OnceLock cache on first call. 3 new tests (707 total). |
| T-027 | S-D | DONE | `df009e1` | `ViewMeta.config_schema` widened to `Option<facet::Shape>` — populated via `<ConfigStruct as facet::Facet>::SHAPE` for primitive views (per T-027 brief). Views without an args struct carry `None`. |
| T-133 | S-E | DONE (library-only) | `PENDING` | `notify::RecommendedWatcher` wiring landed as `SceneFileWatcher` in `crates/scene/src/reload.rs`. Watches the scene file's PARENT dir (non-recursive) — file-level watches break on atomic-save rename-in-place. Background `ark-scene-file-watcher` thread consumes raw notify events through a `std::sync::mpsc`, debounces 200ms (shared with T-043 reconciler debounce), re-hashes via `SceneId::from_file`, emits `SceneFileEvent::Changed(path)` only when content-hash differs from the last observed hash — metadata-only touches silently suppressed. Ignore lists (swap-files + `.#` prefixes) applied in the notify handler closure. 6 deterministic tests: content-change emit, metadata-touch suppression, swap-file ignore, 3-write debounce coalesce, missing-parent start error, clean Drop shutdown. Supervisor wiring intentionally deferred per packet constraint (>50 LOC plumbing threshold) — documented as Phase 7 follow-up. |
| T-128 | S-E | DONE (repurposed) | `PENDING` | `TurnInflightGate` added alongside (not replacing) legacy `ReloadQueue`. Per-extension ref-counted inflight map (`Mutex<HashMap<String,u32>>`); `turn_started(ext)` / `turn_ended(ext)` paired around an ext's mid-turn window (e.g. claude-code `UserPromptSubmit` → `Stop`). `try_gate_or_queue()` returns `GateDecision::Pass` when idle, else `Queued` with pending flag set. `take_pending()` flushes atomically. Saturating decrement tolerates spurious `Stop` events. `clear_ext` drops stuck turns on session-end. If no ext ever calls `turn_started`, the gate is a no-op — reloads pass through immediately (matches "no claude-code-ext loaded, no gate" acceptance). Chose supervisor-side gate (option a) over trait widening (option b). 10 unit tests covering idle pass, queue-on-inflight, nested ref-count, spurious-end no-panic, multi-ext gating, clear_ext, debug impl. Subscriber wiring to `claude-code.user.prompt-submit` / `claude-code.stop` ExtEvents deferred to a supervisor-side follow-up (single `turn_started`/`turn_ended` tokio task; ~20 LOC once the supervisor consumer harness accepts a new subscriber). |
| T-070 | S-B | DONE | `4bad1a4` | `ark bus intent --json <…>` subcommand lands in `crates/cli/src/commands/bus.rs` (~450 LOC incl. tests). Session-id resolution: `--session <frag>` > `$ARK_SESSION_ID` > unique session under `$STATE/sessions/`. 2s socket deadline. `crates/plugins/ark-bus/src/lib.rs` `dispatch_intent` rewired from `PathBuf::from("ark-hook")` → `PathBuf::from("ark")` with `["bus","intent","--json", canonical]`. 23 unit + reduced-e2e tests (real-supervisor gap flagged). |
| T-071 | S-B | DONE | `4bad1a4` | `ark bus emit --json <…>` verb sits under the same module as T-070. Accepts `{event, payload, source}` envelope — `source` defaults to `ext:ark-bus`. `spawn_emit` in ark-bus lib rewired same way (`ark-hook` → `ark bus emit`). Response report includes receiver count for operator feedback. Tests cover intent + emit round-trip against an in-process echo `UnixListener` that mirrors the supervisor's NDJSON envelope. |
| T-044 | S-F | DONE | `bd38e35` | reconciler drift-tolerance integration tests (4 added): retain-flags guarantee, quiet-pass reports `predicates_changed=false`, predicate-flip forces convergence, mode-switch preserves retain flags. Uses MockMux-equivalent `RecordingApplier` — no real zellij. |
| T-065 | S-A | DONE | `bd38e35` | `compile::keybinds` module emits `keybinds { shared { bind "<chord>" { MessagePlugin "ark-bus" { name "ark-intent"; payload "<JSON>" } } } }` block. No `clear-defaults=true` (additive merge). `inject_keybinds_if_needed(doc, ir)` injector mirrors `inject_ark_bus_if_needed` contract. 15 unit tests + snapshot fixture + end-to-end KDL re-parse. Also side-effect: `auto_mount.rs` newly registered in `compile/mod.rs` (was compiled-dead). |
