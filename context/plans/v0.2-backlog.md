---
created: "2026-04-18"
last_edited: "2026-04-18"
---

# v0.2 Backlog

> **v0.2 BACKLOG CLOSE-OUT — 2026-04-18.** All 7 cleanup items (#1..#7)
> landed in a single day. #1 / #2 / #3 / #4 / #5 / #7 shipped as DONE;
> #6 shipped as PARTIAL (MVP scaffold) — the `crates/ark-test-harness/`
> crate + API surface are green, full subagent-burst + view-grid
> assertion suite deferred to v1.0. DRAFT-site initiatives (#8
> supervisor-wiring / #9 mux-tight-coupling / #10 scene-v3 full
> build-out / #11 pi extension family) remain as future v0.2 scope
> and are sized / tracked in their own build sites under
> `context/plans/`.

Items deferred from v0.1 build sites. Consolidated from 4 impl ledgers.
Each item names the flagging site/ledger so future kits trace back.

> **Scene-v3 audit COMPLETE — 2026-04-18.** All 17 pending items landed
> (S-A..S-H × T-027/T-044/T-053/T-057/T-065/T-070/T-071/T-084/T-114/
> T-128/T-133/T-135/T-136/T-137/T-138). T-084 closed as Option-B
> scaffold at `f5475c3` — `WasmExtensionClient` exists as a type so
> ark's supervisor can dispatch `Arc<dyn ExtensionClient>` against any
> of the three delivery modes (in-proc / NDJSON / wasm), but the
> wasmtime runtime integration is deferred to a v0.2 cargo feature
> (`wasm-transport`) per the "no unconditional 10 MB dep" constraint.
> Follow-up items for the wasm path tracked in the T-084 ledger row
> at `context/impl/impl-scene-v3-pending.md`. See loop-log.md
> close-out banner for the full matrix.

## Blocking (fix before v0.2 ships)

### 1. ark-bus → supervisor intent dispatch bridge — DONE
**Source:** `crates/plugins/ark-bus/src/lib.rs` lines 439, 459.
**Flagged by:** Cleanup Packet A (2026-04-18).
**Status:** DONE — scene-v3 S-B (T-070 + T-071), 2026-04-18. Commit `4bad1a4`.
**Fix delivered:** Added `ark bus intent --json <…>` + `ark bus emit --json <…>` subcommands in `crates/cli/src/commands/bus.rs`. Session-id resolution: `--session <frag>` > `$ARK_SESSION_ID` env > unique `$STATE/sessions/<id>/` fallback (ambiguity error otherwise). `crates/plugins/ark-bus/src/lib.rs` rewired to `CommandToRun::path = PathBuf::from("ark")` with `["bus","intent|emit","--json", canonical]`. 23 unit/reduced-e2e tests in `commands::bus::tests`; workspace 2232 → 2255 passing.
**Deferred:** `ARK_SESSION_ID` env propagation into the zellij pane wrapper was NOT added — the unique-session fallback covers the common case. If future multi-session hosts need strict attribution, either wire `ARK_SESSION_ID` through `crates/scene/src/compile/layout.rs` or have ark-bus emit `--session <sid>` explicitly.

### 2. Stack::spawn_pane live RPC wiring — DONE
**Source:** `crates/ark-view/src/typed.rs:252` — `PaneAttrs` was empty struct in v0.1.
**Flagged by:** claude-code-ext Tier 6b T-038 (ledger row in impl-claude-code-ext.md).
**Status:** DONE — 2026-04-18. Commit `7947e4c`.
**Fix delivered:** `PaneAttrs` widened to `{view_attrs: serde_json::Value}` with `#[serde(default)]` so v0.1 empty-`{}` wire frames round-trip cleanly. `PaneAttrs::from_attrs<A: Serialize>` constructor for typed-attrs call sites. Process-global `StackDispatcher` trait + `register_stack_dispatcher` (OnceLock, mirrors `ext_dispatch::CAP_REGISTRY` pattern) lets the supervisor plug a real RPC client at init; `Stack::spawn_pane` consults the dispatcher and falls back to the synthetic-handle stub when unset. Integration test in `crates/ark-view/tests/stack_dispatcher.rs` isolated so the OnceLock doesn't leak into sibling unit tests. No protocol version bump — `{view_attrs: null}` is strict-superset of v0.1 `{}`. See `context/impl/impl-v0.2-backlog.md`.
**Follow-ups:** Supervisor-side `StackDispatcher` impl + registration (Phase 7 wiring). Not blocking this item's closure — the infrastructure is landed + tested.

## Non-blocking (nice-to-have v0.2)

### 3. SubagentRegistry auto-wire into accept loop — DONE
**Source:** `extensions/claude-code/src/view.rs` — `SubagentRegistry` was public + tested but not wired.
**Flagged by:** claude-code-ext Tier 6b T-037 (ledger).
**Status:** DONE — 2026-04-18. Commit `8804b9f`.
**Fix delivered:** Added `subagent_registry: Arc<SubagentRegistry>` + `rename_pane_emitter: Option<Arc<dyn Fn(RenamePaneEmission) + Send + Sync>>` fields on `ClaudeCodeExtension`. `new()` / `with_event_sink()` construct a fresh registry; Clone shares the Arc (asserted via `Arc::ptr_eq`). Accept loop folds the registry on every decoded hook frame BEFORE forwarding to the core bus so downstream reactions see an updated snapshot; invokes the emitter synchronously on each emission so pane-rename UI lands ahead of any async reactive cascade. Builder `with_rename_pane_emitter<F>` for supervisor-side installation. 7 new unit tests in `v0_2_backlog_3_tests`.
**Follow-ups:** Supervisor-side emitter impl that maps `RenamePaneEmission.id` → the corresponding stack-child `Pane<ClaudeCodeSubagent>` handle and dispatches `pane/emit(payload)` against it.

### 4. CLI `ark ext <name> <verb>` dispatcher — DONE
**Source:** `extensions/claude-code/src/lib.rs` — `control_verbs` + `run_install_hooks_verb` + `run_reinstall_hook_binary_verb` + `reload`.
**Flagged by:** claude-code-ext Tier 4 (T-022/T-023) + Tier 8 T-046 (ledger).
**Status:** DONE — 2026-04-18. Commit `4c0bffd`.
**Fix delivered:** `ark ext invoke <name> <verb> [args...]` subcommand in `crates/cli/src/commands/ext/verb.rs` sends `ControlVerbInvoke { ext, verb, args: Vec<String> }` over the control socket. New supervisor module `crates/supervisor/src/ext_verb_dispatch.rs` exposes a `ControlVerbDispatcher` trait + `register_control_verb_dispatcher` OnceLock (mirrors v0.2 #2 `StackDispatcher` pattern). Supervisor `commands.rs` adds `ControlVerbInvoke` command variant that consults the global dispatcher; unwired dispatcher surfaces a clear `"no control-verb dispatcher registered"` error. Session-id resolution mirrors `ark bus`: `--session` > `$ARK_SESSION_ID` > unique session dir fallback. 14 new tests (clap parse, build_request shape, resolve_active_session, render_response ok/err, end-to-end echo-supervisor success + err + missing-socket paths). NO trait widening — the extension's existing verb handlers (`run_install_hooks_verb`, `run_reinstall_hook_binary_verb`, `reload`) become the dispatcher's fan-out target once supervisor-side wiring installs a concrete dispatcher.
**Follow-ups:** Supervisor-side concrete `ControlVerbDispatcher` impl — maps `ext` name to the per-extension handler. Scope deferred here per "do not widen ArkExtension trait" constraint; the infrastructure + CLI + wire contract are all landed + tested.

### 5. ext_state persistence for list columns — DONE
**Source:** `extensions/claude-code/src/columns.rs` — `list_state: Arc<Mutex<CcListColumnState>>` is in-process only.
**Flagged by:** claude-code-ext Tier 7 T-044 (ledger).
**Status:** DONE — 2026-04-18. Commit `d91c55b`.
**Fix delivered:** On-disk sentinel path (kit R1 option 2) — extension writes atomically to `$STATE/sessions/<sid>/ext-state/claude-code.json` on every `fold_transcript_line` / `fold_transcript_blob` update; `ark list` reads every `ext-state/*.json` back and overlays them onto `SessionStatus.ext_state` per session row. `StateLayout::session_ext_state_dir/path` provides canonical paths. `CcListColumnState::write_to_file/read_from_file` handle atomic write (tmp + rename) + tolerant read (missing / malformed → `None`). `ClaudeCodeExtension::configure_ext_state_path` installs the target at `on_session_start` time; write failures log + continue (list columns are observability). 17 new tests — 7 in `columns::tests` (round-trip, missing, malformed, parent-create, atomic-no-tmp-residue), 7 in `v0_2_backlog_5_tests` (default-none, configure, fold-no-path, fold-with-path, cumulative, blob, clone-shared, write-failure-non-fatal), 3 in `cli::list::tests` (empty dir, pickup + non-json skip, malformed skip, end-to-end overlay).
**Follow-ups:** Supervisor-side write path (atomic snapshot of runtime ext_state into `status.json` at shutdown) — kit-R1 option 1; not required now that the sentinel path is authoritative. Can be added later if a second ext needs the supervisor-mediated shape.

### 6. Full-zellij PTY harness for claude-code-ext — PARTIAL (MVP scaffold)
**Source:** `extensions/claude-code/tests/claude_code_smoke.rs` — T-048 runs reduced harness (direct socket + NDJSON, no real zellij launch).
**Flagged by:** claude-code-ext Tier 8 T-048 (ledger).
**Status:** PARTIAL (MVP scaffold) — 2026-04-18. Commit TBD.
**Fix delivered:** New `crates/ark-test-harness/` crate with a reusable `ArkHarness` builder. Lifts the pty + tempdir + session-polling logic from `crates/cli/tests/launch_pty.rs` into a shared library. Public API: `ArkHarness::try_new` (SKIP branch returns `Ok(None)` when zellij missing or nested), `HarnessBuilder` with `with_claude_mock`, `session_name`, `extra_env`, `pty_size` knobs, and `wait_for_ready` / `send_input` / `dump_screen` / `wait_for_text` / `shutdown` on the harness. `stage_claude_shim` provisions a PATH-prepended tempdir with a `claude` symlink → mock-claude-cc. `dump_screen` shells out to `zellij action dump-screen <tmpfile>` with pty-buffer fallback. `PtyProcess` wraps portable-pty with an interior-mutable writer + reader-thread buffer capped at 256 KiB. Workspace 2345 → 2358 passing (+13 new tests); `cargo test -p ark-test-harness` green on hosts without zellij (SKIP-path exercised) and equivalent on zellij-on-PATH hosts outside of zellij (full launch round-trip exercised).
**Deferred (v1.0 follow-up):** Full subagent-burst integration test with view-grid assertions. The MVP ships the scaffold + API surface + one self-smoke-test that exercises launch → ready → shutdown. Downstream crates (extensions/claude-code, crates/cli) can now add `ark-test-harness` as a dev-dep and build richer scenarios on top without re-deriving the pty + tempdir plumbing. No VT100 grid parser shipped — callers that need cell-level assertions should pair the harness's `pty_buffer` / `dump_screen` output with the `vt100` crate in their own dev-deps.
**Effort realized:** M (single packet). L estimate was for the full grid-assertion suite — that deferred piece remains L-effort.

### 7. cc-hook real `include_bytes!` embedding — DONE (via cargo-install sidestep)
**Source:** `crates/cli/build.rs` + `extensions/claude-code/src/lib.rs` — `CC_HOOK_BYTES: &[u8] = &[]` (stub).
**Flagged by:** claude-code-ext Tier 1 T-008a (ledger).
**Status:** DONE — 2026-04-18. Commit `45dc120`.
**Fix delivered:** Took the sidestep option — `install_cc_hook_at` now falls through to `cargo install --bin cc-hook --path <extensions/claude-code> --root <install-root> --locked` when `CC_HOOK_BYTES` is the empty stub. New `InstallOutcome::InstalledViaCargo` variant surfaces the success path. Source tree resolution cascades `$ARK_CLAUDE_CODE_EXT_DIR` > compile-time `CARGO_MANIFEST_DIR` > run-time `$CARGO_MANIFEST_DIR`. `$ARK_CARGO_BIN` overrides the `cargo` binary for shim tests. `$ARK_CLAUDE_CODE_NO_CARGO_FALLBACK=1` opts out entirely so constrained environments can stay deterministic. Install path must be `<root>/bin/cc-hook` (cargo's layout); other shapes fall through to `StubEmpty` with a clear log. 7 new tests using a shell shim script pointed to by `$ARK_CARGO_BIN` — success + non-zero exit + invalid basename + invalid parent + missing cargo + `locate_claude_code_ext_path` env override + compile-time resolution. Env mutations serialized via a module-local `env_lock()` OnceLock mutex to avoid test parallelism flakes. The real `include_bytes!` embedding (cargo-dist style) is NOT implemented — the cargo-install fallback covers the use case for any user with `cargo` on PATH + the source tree; distributed-binary-without-source remains a TODO but is no longer blocking.
**Follow-ups:** cargo-dist / resource-compilation embedding for distributed binaries where `cargo` is absent. Lower priority now that the cargo-install path works for the developer + self-hosted operator.

## Non-cleanup v0.2 initiatives (sourced from plan-overview DRAFT sites)

### 8. Supervisor wiring (Phase 7)
**Source:** `context/plans/build-site-supervisor-wiring.md` (6 tasks, DRAFT).
**Scope:** Pipe-inheritance ready signal. In-process daemon model. `ark run` → real supervisor fork path.

### 9. Mux tight-coupling revision
**Source:** `context/plans/build-site-mux-tight-coupling.md` (13 tasks, DRAFT).
**Scope:** Delete `Multiplexer` trait (no narrow-trait replacement). Concretize `ZellijMux`. Relocate `status_pipe` to supervisor. `ZellijMux::for_test` constructor. Drop `TmuxMux`.

### 10. Scene v3 full build-out
**Source:** `context/plans/build-site-scene.md` (140 tasks, partially superseded by scene-2026-04-18).
**Scope:** 16 tiers across 5 milestones. v0.1 Scene DSL + reconciler (73 tasks) — most landed via scene-2026-04-18 revision (26 tasks) + phase-2 type foundations. Remaining tasks need audit pass.

### 11. pi extension family (deferred 2026-04-18 pivot)
**Source:** `context/kits/cavekit-pi.md` (status=deferred), memory `project_pi_extension.md`.
**Scope:** 3 crates (pi-core / pi-subagents / pi-control). TS bridge over unix socket.

---

**Legend:**
- **Blocking** = must close before v0.2 tag.
- **Non-blocking** = v0.2 scope but tag-eligible without.
