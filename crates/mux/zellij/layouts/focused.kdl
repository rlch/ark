layout {
    tab name="{{ name }}" cwd="{{ cwd }}" {
        pane name="agent" {
            command "{{ agent_cmd }}"
            {% for a in agent_args %}args "{{ a }}"
            {% endfor %}
        }
    }
}
