# crates/test-fixtures/claude-code

Mock `claude` binary fixture — a standalone crate producing a deterministic
stand-in used by `extensions/claude-code` integration tests (smoke + settings
reconcile + view integration).

Implements:
- cavekit-claude-code.md R13 (test-only mock-claude fixture)

Build site: build-site-claude-code-ext.md (T-014..T-018 Tier 3)
Impl tracking: context/impl/impl-claude-code-ext.md
